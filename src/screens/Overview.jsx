export default function Overview() {
  return (
    <div>
      <h1 className="fig" style={{ fontSize: 28, fontWeight: 400, margin: '0 0 8px' }}>
        Overview
      </h1>
      <p style={{ color: 'var(--ink2)', fontSize: 13.5 }}>
        Command center — net worth, cash flow, and needs attention land here next.
      </p>
    </div>
  );
}
